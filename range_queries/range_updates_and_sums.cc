#include <cstdlib>
#include <iostream>

typedef long long ll;

using namespace std;

int main() {
    int n, q = 0; // array size, # of queries
    ll x = 0; // updated value
    cin >> n >> q;
    ll arr[n * 2]; // segment tree setup (index 0 never used)

    
}

/*
g++ -o range_updates_and_sums.out range_updates_and_sums.cc && ./range_updates_and_sums.out
*/